/**
 * config.example.js — Plantilla de configuración de Supabase
 *
 * Pasos:
 *   1. Copiá este archivo como config.js
 *   2. Reemplazá los valores con los de tu proyecto Supabase
 *      (Dashboard → Settings → API)
 *   3. Nunca commitas config.js con valores reales (está en .gitignore)
 *
 * Para encontrar estos valores:
 *   - SUPABASE_URL: Project URL en Settings → API
 *   - SUPABASE_ANON_KEY: anon public key en Settings → API
 *
 * NUNCA uses la service_role key en el frontend.
 */
window.SUPABASE_URL      = 'https://YOUR_PROJECT_REF.supabase.co';
window.SUPABASE_ANON_KEY = 'YOUR_ANON_PUBLIC_KEY_HERE';
